import { SubscriptionsClient, NodeEvent } from '../subscriptions';
export type WsRequestId = string | number;
export declare class WsSubscriptionsClient implements SubscriptionsClient {
    private readonly url;
    private connections;
    private callbacks;
    constructor(baseUrl: string, path: string);
    connect(connectionId?: string): Promise<void>;
    disconnect(connectionId?: string): void;
    subscribe(contextIds: string[], connectionId?: string): void;
    unsubscribe(contextIds: string[], connectionId?: string): void;
    addCallback(callback: (data: NodeEvent) => void, connectionId?: string): void;
    removeCallback(callback: (data: NodeEvent) => void, connectionId?: string): void;
    private handleMessage;
    private getRandomRequestId;
}
//# sourceMappingURL=ws.d.ts.map